->it is an open sourse server-side runtime environment build on chrome's v8 javascript engine.
->Node.js can be used to build different types of application such as command line,web application,chat application and rest apis.
->what are the datatype we are using in node js?
ans:string,number,boolean,undefined,null,object,functions
->Developer of node.js is Ryan Dahl.
Module types:
---------------
1.core Modules
2.local Modules
3.Third party Modules

module.exports:
-----------------
it is a special object which is included in every javascript file in the node.js application by default.
->



